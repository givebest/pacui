import { Button, Breadcrumb, Checkbox, Divider } from 'antd';
import 'antd/dist/antd.css';

export const PButton = Button
export const PBreadcrumb = Breadcrumb

export const PCheckbox = Checkbox

export const PDivider = Divider